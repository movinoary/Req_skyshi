import React from "react";
import * as cssModule from "../../Scss";

const Loading = () => {
  return (
    <div className={cssModule.Components.loading}>
      <h1>Loading...</h1>
    </div>
  );
};

export default Loading;
